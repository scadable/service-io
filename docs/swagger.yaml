basePath: /
definitions:
  api.addDeviceRequest:
    properties:
      type:
        example: random
        type: string
    type: object
  devices.Device:
    properties:
      container_id:
        example: 3518d34547496f2a8c4af44be3c71d7f...
        type: string
      container_name:
        example: adapter-EDIVRWCLGGPGCW7M
        type: string
      container_url:
        example: EDIVRWCLGGPGCW7M.localhost
        type: string
      created_at:
        type: string
      id:
        example: EDIVRWCLGGPGCW7M
        type: string
      image:
        example: registry.digitalocean.com/scadable-container-registry/adapter-mqtt:latest
        type: string
      mqtt_password:
        description: The JSON tag is changed from "-" to "mqtt_password,omitempty"
          to expose it.
        example: aBc12DeF34gH56iJ
        type: string
      mqtt_user:
        description: MQTT credentials, only populated for 'mqtt' type devices.
        example: EDIVRWCLGGPGCW7M
        type: string
      nats_subject:
        example: devices.EDIVRWCLGGPGCW7M.telemetry
        type: string
      status:
        example: running
        type: string
      type:
        example: mqtt
        type: string
    type: object
host: localhost:9090
info:
  contact: {}
  description: This is the API for the service-io device onboarding microservice.
  title: service-io API
  version: "1.0"
paths:
  /devices:
    get:
      description: Retrieves a list of all device adapters from the database.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/devices.Device'
            type: array
        "500":
          description: Internal Server Error
          schema:
            type: string
      summary: List all devices
      tags:
      - devices
    post:
      consumes:
      - application/json
      description: Creates a new device adapter instance, runs its container, and
        returns the device details.
      parameters:
      - description: Device Type
        in: body
        name: device
        required: true
        schema:
          $ref: '#/definitions/api.addDeviceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/devices.Device'
        "400":
          description: Bad Request
          schema:
            type: string
        "500":
          description: Internal Server Error
          schema:
            type: string
      summary: Add a new device
      tags:
      - devices
  /devices/{deviceID}:
    delete:
      description: Stops the device's container and removes its record from the database.
      parameters:
      - description: Device ID
        in: path
        name: deviceID
        required: true
        type: string
      produces:
      - application/json
      responses:
        "204":
          description: No Content
          schema:
            type: string
        "404":
          description: Not Found
          schema:
            type: string
        "500":
          description: Internal Server Error
          schema:
            type: string
      summary: Remove a device
      tags:
      - devices
swagger: "2.0"
